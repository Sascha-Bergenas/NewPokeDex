# NewPokeDex

This repository contains a React + TypeScript Pokedex application.

## Project Location

The application code is in the `PokeDex` folder.

For full setup instructions and feature documentation, see:

- `PokeDex/README.md`
